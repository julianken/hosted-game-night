{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalPassThroughEnv": ["E2E_TESTING", "SENTRY_AUTH_TOKEN", "SENTRY_DSN"],
  "globalEnv": [
    "NEXT_PUBLIC_APP_URL",
    "NEXT_PUBLIC_BINGO_URL",
    "NEXT_PUBLIC_TRIVIA_URL"
  ],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"],
      "env": [
        "NEXT_PUBLIC_APP_URL",
        "NEXT_PUBLIC_BINGO_URL",
        "NEXT_PUBLIC_TRIVIA_URL",
        "NEXT_PUBLIC_SENTRY_DSN",
        "NEXT_PUBLIC_FARO_URL",
        "SENTRY_ORG",
        "SENTRY_PROJECT"
      ]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    },
    "test:run": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    },
    "test:coverage": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    },
    "lint": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "typecheck": {
      "dependsOn": ["^build"],
      "outputs": []
    },
    "analyze": {
      "dependsOn": ["^build"],
      "outputs": [".next/analyze/**"],
      "cache": false,
      "env": ["ANALYZE"]
    },
    "clean": {
      "cache": false
    }
  }
}
